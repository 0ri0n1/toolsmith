{
  "_comment": "MCPO bridge config. Each entry wraps a stdio MCP server as an OpenAPI endpoint.",
  "mcpServers": {
    "nmap": {
      "command": "python",
      "args": ["/app/tool-transport/nmap-server.py"],
      "env": {}
    }
  }
}
